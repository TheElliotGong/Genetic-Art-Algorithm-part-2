"""Test suite for the Voronoi genetic art algorithm.

Kept as a package so helper modules (``tests.reference_impl``, ``tests.helpers``)
can be imported without relying on pytest's sys.path insertion order.
"""
